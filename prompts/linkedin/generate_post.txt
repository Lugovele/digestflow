You are not writing a LinkedIn post from scratch.

You are rewriting the provided digest into a strict format.

Audience:
- founders
- product leaders
- technical operators

AUTHOR CONTEXT

You are writing on behalf of a professional with the following profile:

- Role: {author_role}
- Background: {author_background}
- Focus: {author_focus}
- Voice: {author_voice}

Writing style constraints:
- {style_constraint_1}
- {style_constraint_2}
- {style_constraint_3}

IMPORTANT:
- Do NOT invent personal stories.
- Do NOT mention specific companies unless they are explicitly present in the sources.
- Reflect the thinking style, not biography.

INPUT DATA

Summary:
{summary}

Key points:
{key_points}

Sources:
{sources}

You MUST use only the data above.
Do not ask for additional input.

TARGET FORMAT (MUST FOLLOW EXACTLY)

AI makes workflows faster.

But it doesn't fix them.

In one article, there's a case where a team tries to speed up research briefs.
Prep drops from X to Y
(source label)

Looks like a win.

But [state the problem from the same key point].

So the work just moves.

In another example, [continue with the situation].

(metric)
(source label)

But [state the problem from the same key point].

Same pattern.

Speed improves.
But the system still leaks.

Then there's a case where the team [continue with the situation].

(metric)
(source label)

Feels like we keep trying to speed up the wrong thing.

RULES (STRICT)

1. You MUST follow this structure exactly.
- Do NOT invent your own structure.

2. Each key_point becomes one case.
- For each case, extract:
  - situation
  - metric
  - problem
- The problem must come from the same key_point as the metric.
- Write the problem as a complete short sentence.
- Each case must use its own problem.
- Do NOT reuse a problem from another case.
- Keep the case logic matched:
  - research case -> review / claims
  - support case -> labels / routing
  - redesign case -> improvement
- Keep the case structure logically consistent:
  - case 1 -> problem 1
  - case 2 -> problem 2
  - case 3 -> improvement
- Do NOT mix problems between cases.

3. Use 2 to 3 cases only.
- If there are more than 3 key points, pick the strongest ones.

4. Sentences MUST be short.
- Max 10 to 12 words per sentence.
- One idea per sentence.

5. Use line breaks like the example.
- Keep the rhythm short.
- Keep the spacing simple.
- Use natural transition phrases like:
  - "In another example, ..."
  - "There's also a case where..."
  - "Then there's a case where the team..."
- Avoid placeholder phrasing like:
  - "In another case..."
  - "Then there's a different example..."
  - "In another example..."
  - "Then there's a case where..."

6. Inline references are mandatory for every number.
- Every number, percentage, time reduction, or metric must have a reference.
- Put the reference right below the metric line.
- Use this format:
  - (TechCrunch - AI workflow case)
  - (support ops - intake forms)
  - (workflow redesign - ops team)
- Do NOT use "(source: ...)".
- Prefer references like:
  - (AI briefing workflow case)
  - (support ops - intake forms)
- Make references feel like natural context, not metadata.
- Do NOT use raw URLs in post_text.
- Do NOT use full links.
- Do NOT use "source 1", "source 2", or numbering.
- Build the label from the content of the case.
- The label must sound like real context, not a placeholder.
- If no real source label can be generated from the provided data, remove the reference entirely.
- Do NOT output placeholders like:
  - (source label)
  - (source 1)
  - any placeholder reference

7. Use key_points as the source for cases.
- Each case should come from one key point.
- Use the sources list and case content to build short readable labels.
- Each case must include:
  - a metric from the key point
  - a concrete problem from the same key point

8. Hard bans.
- Do NOT use:
  - "for example"
  - "meanwhile"
  - "furthermore"
  - "it's interesting"
  - "this shows"
  - "this demonstrates"
  - "can change dramatically"
  - "influence outcomes"
  - "But..."
  - "But it..."
  - incomplete sentences

9. Do NOT:
- explain
- summarize
- generalize
- sound like an article

10. Humanization layer.
- Rewrite every sentence so it sounds like natural spoken thought.
- Prefer simple language over formal phrasing.
- Prefer concrete wording over abstract wording.
- Use human phrasing like:
  - "someone still has to check everything"
  - "labels are still messy"
  - "wrong claims slip through"
- Prefer simple metric phrasing like:
  - "gets 28% faster"
  - "drops by"
- Replace unnatural phrases with spoken equivalents:
  - "has inconsistencies" -> "someone still checks"
  - "reduces significantly" -> "gets faster"
  - "shortens well" -> "drops"
- Avoid stiffer phrasing like:
  - "cuts by 28%"
  - "reduces by"
- Avoid formal phrasing like:
  - "inconsistencies in editor review lead to errors"
  - "the system fails to standardize intake messages"
- Add short reaction lines where they help the rhythm:
  - "Looks like a win."
  - "Same pattern."
  - "Still breaks."
  - "So nothing really changes."
- Each case must also include one short follow-up line that keeps the flow moving.
- Good follow-up lines:
  - "So nothing really disappears."
  - "So the work just moves."
  - "Speed improves."
  - "Still breaks."
- Each case should feel like:
  - example
  - reaction
  - problem
  - follow-up line
- Do not repeat the same short sentence twice.
- If a short reaction appears once, do not reuse the exact same line later.
- Add small context phrases so each case feels like continuation, not reset:
  - "a team changes how..."
  - "the team starts from..."

11. Style check.
- If the text reads like:
  - a blog post
  - a LinkedIn article
  - an explanation
  rewrite it.
- Do not repeat the same idea twice in one case.
- If two sentences express the same meaning, keep only one.
- Focus on outcomes, not mechanisms.
- Good:
  - "triage gets 28% faster"
- Bad:
  - "structured intake forms improve efficiency"

12. Final check before returning.
- All numbers have references.
- The rhythm matches the example.
- The post uses ONLY the provided input data.
- The text reads like one connected thought.
- Every "But" line contains a concrete problem from the same key point.
- If a "But" line does not contain a concrete problem, rewrite the whole case.
- If a case uses a problem from another case, rewrite that case.
- If case logic is mismatched, rewrite the case.
- End with only ONE final insight line.
- Do NOT add extra closing sentences after the final insight.
- No case repeats the same idea twice.
- Outcome wording is stronger than mechanism wording.
- No identical short reaction line appears twice.
- Each case feels like continuation, not reset.
- Ask:
  - would a real person say this out loud?
  - does it sound like thinking, not writing?
- If not, rewrite it.

Output rules:
- Return valid JSON only.
- Do not wrap the JSON in markdown fences.
- Do not add comments, explanations, or any text before or after the JSON.
- post_text must follow the strict format above.
- hook_variants must contain exactly 3 items.
- hook_variants must not be questions.
- hook_variants should be short and direct.
- hook_variants must match the tone of post_text.
- Use:
  - short sentences
  - contrast
  - simple words
- Hooks must avoid abstract phrasing.
- Avoid:
  - "risks linger"
  - "efficiency improves"
- Prefer:
  - "something still breaks"
  - "problems just move"
- Good hooks:
  - "AI makes things faster. But it doesn't fix them."
  - "Looks like a win. But something still breaks."
- Bad hooks:
  - abstract
  - academic
  - generic
- cta_variants must contain exactly 3 items.
- cta_variants should be soft and conversational, not salesy.
- cta_variants must connect to the core idea of the post.
- cta_variants must be specific, not generic.
- cta_variants should sound like real questions.
- Avoid:
  - "Let's discuss"
  - "What are your thoughts"
  - "How do you approach"
- Prefer:
  - "What still breaks?"
  - "Where does it fail?"
  - "Are you fixing or just speeding up?"
- hashtags must be relevant to the provided digest.
- carousel_outline may be short, but must stay grounded in the provided digest.
- quality_checks must reflect whether the output stayed factual and LinkedIn-ready.

Return this exact JSON shape:
{{
  "post_text": "string",
  "hook_variants": ["string", "string", "string"],
  "cta_variants": ["string", "string", "string"],
  "hashtags": ["string"],
  "carousel_outline": [
    {{"slide": 1, "title": "string", "bullets": ["string"]}}
  ],
  "quality_checks": {{
    "uses_only_provided_facts": true,
    "has_clear_point_of_view": true,
    "linkedin_ready": true
  }}
}}

Topic:
{topic_name}

Digest title:
{digest_title}

Digest summary:
{digest_summary}
