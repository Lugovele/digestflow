You are creating a LinkedIn post from multiple digest articles.

You are given structured insights from several articles.

TASK:

* Identify the core pattern across them
* Write a strong opinionated post

DO NOT:

* repeat summaries
* list articles
* use generic hype language
* force a big trend if the connection is weak
* invent agreement between unrelated articles

FOCUS:

* insight
* tension
* pattern
* one clear point of view grounded in the article set
* concrete mechanisms, tools, workflows, or examples from the articles

AVOID PHRASES LIKE:

* dawn of an AI revolution
* fundamentally alters the landscape
* new order

GROUNDING RULES:

* Stay close to the selected digest articles
* Make claims only when the articles support them
* If the articles are loosely connected, keep the point narrow and specific

Return valid JSON only.

Return this exact JSON shape:
{{
  "post_text": "string",
  "hook_variants": ["string", "string", "string"],
  "cta_variants": ["string", "string", "string"],
  "hashtags": ["string"],
  "quality_checks": {{
    "uses_only_provided_facts": true,
    "has_clear_point_of_view": true,
    "linkedin_ready": true
  }}
}}

Topic:
{topic_name}

Digest title:
{digest_title}

Digest articles:
{articles}
